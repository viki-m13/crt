# Downloaded market data (too large for git, re-downloaded by scan)
data/*.csv
data/download_manifest.json
crypto/data/*.csv
crypto/data/download_manifest.json

# Python cache
__pycache__/
*.pyc

# Cloned reference repos
lib/agenthub/
lib/autoresearch/

# Run logs
*.log
run.log

# v2 strategy: large derived caches (regenerable from features/ via build_dataset.py)
monthly_dca/cache/v2/panel_cross_section*.parquet

# v3 universes: large derived cross-sections (regenerable via run_universe / build_sp500_extended_panel)
monthly_dca/cache/v3_universes/*/panel_cross_section*.parquet
